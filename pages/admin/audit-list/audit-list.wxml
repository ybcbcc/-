<view class="page-container">
  <filter-bar options="{{filterOptions}}" selectedKey="{{selectedKey}}" bind:change="onFilterChange" />
  <info-list list="{{list}}" bind:click="handleItemClick" emptyText="暂无抽奖记录"></info-list>
</view>
