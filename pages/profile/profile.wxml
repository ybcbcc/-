<view class="page-container">
  <!-- 顶部个人信息区域 -->
  <view class="user-header">
    <view class="user-info" bindtap="handleLogin">
      <view class="avatar-container">
        <image class="avatar" src="{{userInfo.avatarUrl || ''}}" mode="aspectFill"></image>
        <view wx:if="{{!userInfo.avatarUrl}}" class="avatar-placeholder"></view>
      </view>
      <text class="nickname">{{userInfo.nickName}}</text>
    </view>
  </view>

  <!-- 菜单列表区域 -->
  <view class="menu-list">
    <view class="menu-item" wx:for="{{menuList}}" wx:key="id" bindtap="handleMenuClick" data-item="{{item}}">
      <!-- 如果有图标可以放在这里 -->
      <view class="menu-icon-placeholder"></view>
      
      <text class="menu-title">{{item.title}}</text>
      
      <!-- 特殊处理：如果是积分，显示数字且不显示箭头 -->
      <block wx:if="{{item.id === 'points'}}">
        <text class="points-value">{{userInfo.points}}</text>
      </block>
      <block wx:else>
        <!-- 右侧箭头 -->
        <view class="arrow-right"></view>
      </block>
    </view>
  </view>
</view>
